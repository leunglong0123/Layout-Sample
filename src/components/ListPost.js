import * as React from "react"

// const ClickHandler = () => {

// }
// markup
const ListPost = (props) => {
  return (
    <div>
      <h1>{props.post.title}</h1>
      <hr />
    </div>
  )
}

export default ListPost
